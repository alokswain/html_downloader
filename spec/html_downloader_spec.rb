require "spec_helper"

describe HtmlDownloader do
  it "has a version number" do
    expect(HtmlDownloader::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
